const FORMATS = Object.freeze({
	fullDateTime: 'YYYY-MM-DD HH:mm:ss',
});

const SENDERS = Object.freeze({
	admin  : 'admin',
	server : 'server',
	client : 'client',
});

module.exports = {
	FORMATS,
	SENDERS,
};
