import React from 'react';

import { Collapse, CollapsePanel } from '../../../components/lib';

const Settings = () => {
	return (
		<Collapse>
			<CollapsePanel header="Filters & Settings" key="1">
				Filters & Settings
			</CollapsePanel>
		</Collapse>
	);
};

export default Settings;
